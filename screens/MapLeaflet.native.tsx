// Native stub - not used on native, maps are handled by react-native-maps in the native screen
import React from 'react';
import { View } from 'react-native';

export default function MapLeaflet() {
    return <View />;
}
